- flash.each do |type, msg|
  %div.text-center{class: "alert #{type == 'notice' ? 'bg-info' : 'bg-danger'}"}
    %strong= msg
    %button.close{"aria-label" => "Close", :type => "button", 'data-dismiss' => 'alert'}
      %span{"aria-hidden" => "true"} ×
